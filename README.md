daily_backup.sh -- Backs up a folder to another drive, zip-encrypted with timestamped filenames.

rename_files_by_date.py -- Renames files in a folder based on their creation/modification date.

auto_clean_temp.sh -- Deletes old files from /tmp, Downloads, or custom directories.

markdown_indexer.py -- Builds an index of .md files with titles/headers as TOC.

wifi_auto_toggle.sh -- Turns Wi-Fi off when Ethernet is connected, and vice versa.
